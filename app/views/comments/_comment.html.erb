<p><strong>Usuario: </strong><%= comment.commenter%>  </p>
<p><strong>Comentario: </strong><%= comment.body %> </p>
<% if ((current_user.username == @post.user.username) || (current_user.username == comment.commenter))%>
<p>
  <%= link_to 'Eliminar Comentario', [comment.post, comment], 
    :confirm => '¿Seguro que desea eliminar este comentario?',
    :method => :delete,  
    class: :"btn btn-mini btn-danger active"%>
</p>
<% end %>
<hr style="border:1px dashed blue" />